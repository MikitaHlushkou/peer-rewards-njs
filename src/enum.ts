export enum INPUT_TYPES {
  textArea,
  input,
  select,
}

export enum QUERY_KEYS {
  rewards,
  userRewards,
  usersList,
  userProfile,
}

export enum AUTH_CREDS_PROVIDERS {
  google = "google",
  credentials = "credentials",
}
